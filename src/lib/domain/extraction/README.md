# Extraction

This entire module is AI-generated, vibe-coded slop.

It would be lovely if, one day, someone with plenty of experience could revise this process and make it more deterministic & actually properly build/document it.