services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: taxokeys
    ports: ["5433:5432"]
    volumes: ["db_data:/var/lib/postgresql/data"]
volumes:
  db_data:
